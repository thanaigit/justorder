import 'package:flutter/material.dart';

class NullBox extends StatelessWidget {
  const NullBox({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 0.0, width: 0.0);
}
